# Data

The pipeline downloads the official UCI MADELON files into `data/raw/`.

The official `madelon_valid.*` split is used as the final held-out test set in
this course project. The unlabeled official test split is not needed.

