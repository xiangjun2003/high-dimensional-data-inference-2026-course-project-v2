# High-Dimensional Data Inference 2026 Course Project

This is the fresh repository for the revised course project with a new dataset.

The previous RNA-Seq project repository is intentionally preserved separately.
This workspace keeps the local `.venv` directory, but the new project
dependencies and dataset choice are still to be decided.
